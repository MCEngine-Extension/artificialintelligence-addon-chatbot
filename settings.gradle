rootProject.name = 'mcengine-addon-chat-bot'

// Define base directories for subprojects
def coreProjects = ['mcengine-chat-bot-api', 'artificialintelligence']
coreProjects.each {
    include ":$it"
}
