rootProject.name = 'mcengine-artificialintelligence-addon-chat-bot'

// Define base directories for subprojects
def coreProjects = ['artificialintelligence']
coreProjects.each {
    include ":$it"
}